Algoritmo "Tabuada"

Var

 x, y, n : inteiro

Inicio

  escreva("Número desejado da tabuada: ")
  leia(n)
  
  x <- n
  
  para n de 1 ate 10 faca
     y <- x * n
     escreval(x, " x", n, " = ", y)
  fimpara

Fimalgoritmo